{{#with data}}

  {{#with footer}}
    <div class='frame_page'>
      {{#each pages}}

        <a class='footer_pages' href='{{path}}'>{{name}}</a>

      {{/each}}
    </div>
    <div>
      <span class='footer_copyright'>{{copyright}}</span>
    </div>
  {{/with}}

{{/with}}